#include <iostream>
#define ll long long int
using namespace std;
 
int main() {
	int t;
	cin >> t;
	while(t--)
	{
		ll m,n;
		cin >> m >> n;
		if(m-n!=1)
		cout << "YES\n";
		else
		cout << "NO\n";
	}
	return 0;
}
